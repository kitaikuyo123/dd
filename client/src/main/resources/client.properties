# 客户端配置文件

# ZooKeeper 地址
zookeeper.connect=localhost:2181

# 连接池配置
connection.pool.size=10
connection.pool.max.size=50
connection.timeout.ms=10000

# 请求路由配置
router.retry.times=3
router.retry.interval.ms=1000

# 日志配置
log.level=INFO
log.file=./logs/client.log
